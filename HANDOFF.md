- Done: Refocused the Connection Hub into the AGM Codex Onboarding pilot; seeded only Stevie and Jean; added database-managed invitations, role assignments, approval/denial, revocation, audit history, staff-scoped setup, seven onboarding/read-only Meta MCP tools, a public Codex marketplace plugin, Mac/Windows installers, and durable Higgsfield installation. Published `Roger-Roger-StevieAI/agm-codex-onboarding` publicly and deployed Sites version 4 to the existing production URL. Repaired Stevie's four broken Higgsfield skill links and verified the existing local CLI/account without generating or spending credits.
- Files changed: `app/`, `db/`, `drizzle/`, `plugins/agm-codex-onboarding/`, `.agents/plugins/marketplace.json`, `install/`, `README.md`, `tests/rendered-html.test.mjs`, `cloudflare-env.d.ts`, and `HANDOFF.md`. Removed the old `plugins/connection-hub/` package.
- Validation: Typecheck, ESLint, production build, four Node tests, shell syntax, plugin validation, local dashboard/API authorization, staff request/admin approval, immediate deactivation, MCP initialize, and MCP tools/list pass. Jean is restored to Active after the revocation test. Higgsfield CLI 0.2.3 and all four durable skill files were verified locally.
- Blockers: AGM must still create/authorize the Meta developer app in Composio and set `COMPOSIO_API_KEY` plus `COMPOSIO_META_CONNECTED_ACCOUNT_ID` as server-only Sites variables. Jean must run the public installer, sign into his own Higgsfield account, and approve one inexpensive test generation; those external-account acceptance checks cannot be completed from Stevie's machine.
- Next agent action: Complete AGM Meta OAuth in Composio, add the two protected Sites variables, use the hub's read-only Meta test, then have Jean run the Mac or Windows installer and return the completed Higgsfield result URL through `hub_test_connection`.
