- Done: Upgraded the AGM Connection Hub into an operational control plane. Administrators can create provider definitions, launch secure Composio Connect Links, run allowlisted read-only diagnostics, retain sanitized provider logs, create/edit/delete role templates, invite staff, send/revoke individual connections, copy the staff setup link, and disable users. Staff can connect/test their assigned capabilities and requests now approve as individual deliveries instead of silently expanding a whole role template. Added Gmail, Drive, Sheets, and Meta provider seeds, persistent provider configuration, member-provider state, and diagnostic history while keeping credentials outside D1 and the public installer.
- Files changed: `app/ConnectionHub.tsx`, `app/api/hub/route.ts`, `app/composio-runtime.ts`, `app/connection-runtime.ts`, `app/globals.css`, `db/hub.ts`, `db/schema.ts`, `drizzle/0002_wooden_cyclops.sql`, Drizzle metadata, tests, README, `.gitignore`, and this handoff.
- Validation: TypeScript, ESLint, production build, five Node tests, migration generation, diff checks, and local authenticated API workflows pass. End-to-end validation created/updated/deleted a temporary template; directly delivered Gmail to Jean and confirmed his scoped snapshot; stored an expected provider diagnostic; exercised Jean request to Stevie approval to individual Google Sheets delivery; and cleaned up every temporary assignment/template. The local snapshot exposes 26 connection definitions. No provider credential was added to source or D1. Sites version 7 is live at `https://agm-connection-hub.stevie926063.chatgpt.site`; production environment revision 3 contains a protected, least-privilege Composio project key. Live verification shows the provider gateway as Operational, and the Gmail connected-account plus `GMAIL_GET_PROFILE` read-only diagnostic passed and was retained in the hub.
- Blockers: Meta still requires AGM's Meta developer app/custom auth configuration and the legacy MCP adapter needs `COMPOSIO_META_CONNECTED_ACCOUNT_ID`. Jean still needs to run the public installer and complete his own Higgsfield test generation.
- Next agent action: Configure/authorize the Meta Ads auth configuration, then use Connections > Set up & debug to verify account listing. Have Jean sign in, run the installer, and complete the Higgsfield result-URL check.

## 2026-07-14 — Codex — Simplified staff invite and assignment flow

- Done: Reworked Staff > Invite so Stevie can choose a saved template or select a custom connection bundle in the same form. Custom bundles use a protected blank `Custom connections` template plus direct assignments created atomically with the invite. Staff now see the full assigned package as soon as they sign into the hub; the existing Manage panel remains for later exceptions.
- Files changed: `app/ConnectionHub.tsx`, `app/api/hub/route.ts`, `app/globals.css`, `db/hub.ts`.
- Validation: TypeScript, ESLint, production build, and all five Node tests pass. Published production version 8 at `https://agm-connection-hub.stevie926063.chatgpt.site`.
- Blockers: No blocker for the invite workflow. Existing provider-specific blockers remain: Meta OAuth, individual Higgsfield login/test, and Google Sheets authorization.
- Next agent action: Use Staff > Invite and assign to onboard future staff in one step; choose `Custom connections` when a reusable template is not needed.
